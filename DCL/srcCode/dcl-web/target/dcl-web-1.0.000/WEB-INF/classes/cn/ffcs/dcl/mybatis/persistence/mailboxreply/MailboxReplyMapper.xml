<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.ffcs.dcl.mybatis.persistence.mailboxreply.MailboxReplyMapper">
	<resultMap id="BaseResultMap" type="cn.ffcs.dcl.mybatis.domain.mailboxreply.MailboxReply">
		<id column="MAILBOX_REPLY_ID" property="mailboxReplyId" jdbcType="DECIMAL" /> <!--主键-->
		<result column="UUID" property="uuid" jdbcType="VARCHAR" /> <!--UUID-->
		<result column="MAILBOX_UUID" property="mailboxUuid" jdbcType="VARCHAR" /> <!--书记信箱唯一标识-->
		<result column="REPLY_ID" property="replyId" jdbcType="VARCHAR" /> <!--回复人ID-->
		<result column="REPLY_NAME" property="replyName" jdbcType="VARCHAR" /> <!--回复人-->
		<result column="REPLY" property="reply" jdbcType="VARCHAR" /> <!--回复意见-->
		<result column="REPLY_TIME" property="replyTime" jdbcType="TIMESTAMP" /> <!--回复时间-->
		<result column="VISIBLE" property="visible" jdbcType="VARCHAR" /> <!--是否全员可见-->
		<result column="REMARK" property="remark" jdbcType="VARCHAR" /> <!--备注-->
		<result column="IS_VALID" property="isValid" jdbcType="VARCHAR" /> <!--是否有效-->
		<result column="CREATOR" property="creator" jdbcType="DECIMAL" /> <!--创建人-->
		<result column="CREATE_TIME" property="createTime" jdbcType="TIMESTAMP" /> <!--创建时间-->
		<result column="UPDATOR" property="updator" jdbcType="DECIMAL" /> <!--修改人-->
		<result column="UPDATE_TIME" property="updateTime" jdbcType="TIMESTAMP" /> <!--修改时间-->
	</resultMap>
		
	<!-- 列表（总数） -->	
	<select id="countList" resultType="java.lang.Long">
		SELECT COUNT(1) FROM T_DV_MAILBOX_REPLY T
		<include refid="find_where" />
	</select>
	
	<!-- 列表（分页） -->	
	<select id="searchList" resultMap="BaseResultMap">
		SELECT
			T.UUID,
			T.MAILBOX_UUID,
			T.REPLY_ID,
			T.REPLY_NAME,
			T.REPLY,
			T.REPLY_TIME,
			T.VISIBLE,

			t2.REGION_CODE,
			t2.CLAIMANT,
		    t2.TEL,
		    t2.APPEAL_TIME
		FROM
			T_DV_MAILBOX_REPLY T
		<include refid="find_where" />
		ORDER BY 
			t2.APPEAL_TIME DESC
	</select>
	
	<!-- 列表（条件） -->	
	<sql id="find_where">
		INNER JOIN
			T_DV_SECRETARY_MAILBOX t2 ON T.MAILBOX_UUID = t2.UUID AND t2.IS_VALID = '1'

		<where>
			T.IS_VALID = '1'

			<if test="claimant != null and claimant != '' ">
				<![CDATA[ AND t2.CLAIMANT LIKE '%' || #{claimant, jdbcType=VARCHAR} || '%' ]]>
			</if>
			<if test="visible != null and visible != '' ">
				<![CDATA[ AND T.VISIBLE = #{visible, jdbcType=VARCHAR} ]]>
			</if>
		</where>
	</sql>
	
	<!-- 新增 -->	
	<insert id="insert" parameterType="cn.ffcs.dcl.mybatis.domain.mailboxreply.MailboxReply">
		<selectKey keyProperty="mailboxReplyId" resultType="long" order="BEFORE"> 
			SELECT SEQ_DV_MAILBOX_REPLY.NEXTVAL FROM DUAL
		</selectKey>
		INSERT INTO
			T_DV_MAILBOX_REPLY
		(
			MAILBOX_REPLY_ID,
			UUID,
			MAILBOX_UUID,
			REPLY_ID,
			REPLY_NAME,
			REPLY,
			REPLY_TIME,
			VISIBLE,
			REMARK,
			IS_VALID,
			CREATOR,
			CREATE_TIME,
			UPDATOR,
			UPDATE_TIME
		) VALUES (
			#{mailboxReplyId,jdbcType=DECIMAL},
			#{uuid,jdbcType=VARCHAR},
			#{mailboxUuid,jdbcType=VARCHAR},
			#{replyId,jdbcType=VARCHAR},
			#{replyName,jdbcType=VARCHAR},
			#{reply,jdbcType=VARCHAR},
			sysdate,
			#{visible,jdbcType=VARCHAR},
			#{remark,jdbcType=VARCHAR},
			'1',
			#{creator,jdbcType=DECIMAL},
			sysdate,
			#{updator,jdbcType=DECIMAL},
			sysdate
		)
	</insert>
	
	<!-- 修改 -->	
	<update id="update" parameterType="cn.ffcs.dcl.mybatis.domain.mailboxreply.MailboxReply">
		UPDATE
			T_DV_MAILBOX_REPLY
		<set> 
			<if test="uuid != null " >
				UUID=#{uuid,jdbcType=VARCHAR},
			</if>
			<if test="mailboxUuid != null " >
				MAILBOX_UUID=#{mailboxUuid,jdbcType=VARCHAR},
			</if>
			<if test="replyId != null " >
				REPLY_ID=#{replyId,jdbcType=VARCHAR},
			</if>
			<if test="replyName != null " >
				REPLY_NAME=#{replyName,jdbcType=VARCHAR},
			</if>
			<if test="reply != null " >
				REPLY=#{reply,jdbcType=VARCHAR},
			</if>
			<if test="replyTime != null " >
				REPLY_TIME=#{replyTime,jdbcType=TIMESTAMP},
			</if>
			<if test="visible != null " >
				VISIBLE=#{visible,jdbcType=VARCHAR},
			</if>
			<if test="remark != null " >
				REMARK=#{remark,jdbcType=VARCHAR},
			</if>
			<if test="isValid != null " >
				IS_VALID=#{isValid,jdbcType=VARCHAR},
			</if>
			<if test="updator != null " >
				UPDATOR=#{updator,jdbcType=DECIMAL},
			</if>
		 UPDATE_TIME = sysdate
		</set>
		WHERE
			MAILBOX_REPLY_ID= #{mailboxReplyId,jdbcType=DECIMAL} 
	</update>
	
	<!-- 详情 -->	
	<select id="searchByUuid" parameterType="java.lang.String" resultMap="BaseResultMap">
		SELECT
			T.UUID,
			T.MAILBOX_UUID,
			T.REPLY_ID,
			T.REPLY_NAME,
			T.REPLY,
			T.REPLY_TIME,
			T.VISIBLE,

			t2.REGION_CODE,
			t2.CLAIMANT,
			t2.TEL,
			t2.APPEAL_TIME
		FROM
			T_DV_MAILBOX_REPLY T
		INNER JOIN
			T_DV_SECRETARY_MAILBOX t2 ON T.MAILBOX_UUID = t2.UUID AND t2.IS_VALID = '1'
		WHERE
			UUID = #{uuid,jdbcType=VARCHAR}
	</select>
	
	<!-- 删除 -->	
	<delete id="delete" parameterType="cn.ffcs.dcl.mybatis.domain.mailboxreply.MailboxReply">
		UPDATE
			T_DV_MAILBOX_REPLY
		SET IS_VALID = '0', UPDATE_TIME = SYSDATE, UPDATOR = #{updator,jdbcType=DECIMAL}
	</delete>

	<!-- 获取uuid -->
	<select id="getUuid" resultType="String">
		SELECT SYS_GUID() FROM DUAL
	</select>
</mapper>