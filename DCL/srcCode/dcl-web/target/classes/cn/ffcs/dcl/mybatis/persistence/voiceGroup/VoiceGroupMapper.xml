<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.ffcs.dcl.mybatis.persistence.voiceGroup.VoiceGroupMapper">
	<resultMap id="BaseResultMap" type="cn.ffcs.dcl.mybatis.domain.voiceGroup.VoiceGroup">
		<id column="GROUP_ID" property="groupId" jdbcType="DECIMAL" />
		<result column="ORG_CODE" property="orgCode" jdbcType="VARCHAR" /> <!--所属行政村编码-->
		<result column="UUID" property="uuid" jdbcType="VARCHAR" /> <!--UUID-->
		<result column="ORG_NAME" property="orgName" jdbcType="VARCHAR" /> <!--所属行政村名称-->
		<result column="REMARK" property="remark" jdbcType="VARCHAR" /> <!--备注-->
		<result column="IS_VALID" property="isValid" jdbcType="VARCHAR" /> <!--是否有效-->
		<result column="CREATOR" property="creator" jdbcType="DECIMAL" /> <!--创建人-->
		<result column="CREATE_TIME" property="createTime" jdbcType="TIMESTAMP" /> <!--创建时间-->
		<result column="UPDATOR" property="updator" jdbcType="DECIMAL" /> <!--修改人-->
		<result column="UPDATE_TIME" property="updateTime" jdbcType="TIMESTAMP" /> <!--修改时间-->
		<result column="NAME" property="name" jdbcType="VARCHAR" /> <!--名字-->
	</resultMap>
		
	<!-- 列表（总数） -->	
	<select id="countList"  resultType="java.lang.Long">
		SELECT COUNT(1) FROM T_VOICE_GROUP T
		<include refid="find_where" />
	</select>
	
	<!-- 列表（分页） -->	
	<select id="searchList"  resultMap="BaseResultMap">
		SELECT
			T.GROUP_ID,
			T.ORG_CODE,
			T.UUID,
			T.ORG_NAME,
			T.REMARK,
			T.NAME
		FROM
			T_VOICE_GROUP T
		<include refid="find_where" />
		ORDER BY
		T.UPDATE_TIME DESC
	</select>
	
	<!-- 列表（条件） -->	
	<sql id="find_where">
		<where>
			T.IS_VALID = '1'

			<if test="orgCode != null and orgCode != ''" >
				<![CDATA[ AND T.ORG_CODE LIKE #{orgCode,jdbcType=VARCHAR}||'%' ]]>
			</if>
		</where>
	</sql>
	
	<!-- 新增 -->	
	<insert id="insert" parameterType="cn.ffcs.dcl.mybatis.domain.voiceGroup.VoiceGroup">
		<selectKey keyProperty="groupId" resultType="long" order="BEFORE"> 
			SELECT SEQ_T_VOICE_GROUP.NEXTVAL FROM DUAL
		</selectKey>
		INSERT INTO
			T_VOICE_GROUP
		(
			GROUP_ID,
			ORG_CODE,
			UUID,
			ORG_NAME,
			REMARK,
			IS_VALID,
			CREATOR,
			CREATE_TIME,
			UPDATOR,
			UPDATE_TIME,
			NAME
		) VALUES (
			#{groupId,jdbcType=DECIMAL},
			#{orgCode,jdbcType=VARCHAR},
			sys_guid(),
			#{orgName,jdbcType=VARCHAR},
			#{remark,jdbcType=VARCHAR},
			'1',
			#{creator,jdbcType=DECIMAL},
			sysdate,
			#{updator,jdbcType=DECIMAL},
			sysdate,
			#{name,jdbcType=VARCHAR}
		)
	</insert>
	
	<!-- 修改 -->	
	<update id="update" parameterType="cn.ffcs.dcl.mybatis.domain.voiceGroup.VoiceGroup">
		UPDATE
			T_VOICE_GROUP
		<set> 
			<if test="orgCode != null " >
				ORG_CODE=#{orgCode,jdbcType=VARCHAR},
			</if>
			<if test="uuid != null " >
				UUID=#{uuid,jdbcType=VARCHAR},
			</if>
			<if test="orgName != null " >
				ORG_NAME=#{orgName,jdbcType=VARCHAR},
			</if>
			<if test="remark != null " >
				REMARK=#{remark,jdbcType=VARCHAR},
			</if>
			<if test="isValid != null " >
				IS_VALID=#{isValid,jdbcType=VARCHAR},
			</if>
			<if test="updator != null " >
				UPDATOR=#{updator,jdbcType=DECIMAL},
			</if>
			<if test="name != null " >
				NAME=#{name,jdbcType=VARCHAR},
			</if>
		 UPDATE_TIME = sysdate
		</set>
		WHERE
		UUID= #{uuid,jdbcType=VARCHAR}
	</update>
	
	<!-- 详情 -->	
	<select id="searchById" parameterType="long"  resultMap="BaseResultMap">
		SELECT
			T.GROUP_ID,
			T.ORG_CODE,
			T.UUID,
			T.ORG_NAME,
			T.REMARK,
			T.IS_VALID,
			T.CREATOR,
			T.CREATE_TIME,
			T.UPDATOR,
			T.UPDATE_TIME,
			T.NAME
		FROM
			T_VOICE_GROUP T
		WHERE
			GROUP_ID= #{groupId,jdbcType=DECIMAL} 
	</select>

	<!-- 详情 -->
	<select id="searchByUUId" parameterType="String"  resultMap="BaseResultMap">
		SELECT
			T.GROUP_ID,
			T.ORG_CODE,
			T.UUID,
			T.ORG_NAME,
			T.REMARK,
			T.NAME
		FROM
			T_VOICE_GROUP T
		WHERE
			UUID= #{uuid,jdbcType=VARCHAR}
	</select>
	
	<!-- 删除 -->	
	<delete id="delete" parameterType="cn.ffcs.dcl.mybatis.domain.voiceGroup.VoiceGroup">
		UPDATE
			T_VOICE_GROUP
		SET IS_VALID = '0', UPDATE_TIME = SYSDATE, UPDATOR = #{updator,jdbcType=DECIMAL}
		WHERE
			UUID= #{uuid,jdbcType=VARCHAR}
	</delete>
</mapper>